use midium_core::types::Action;
use tracing::warn;

/// Execute a shortcut action — stub for Phase 5.
pub fn execute_shortcut(action: &Action) {
    warn!(?action, "Shortcut execution not yet implemented (Phase 5)");
}
